---
apiVersion: v1
kind: Service
metadata:
  labels:
    name: ref-data-gov-tool-prototype-service
  name: ref-data-gov-tool-prototype-service
spec:
  ports:
  - name: https
    port: 443
    targetPort: 3000
  - name: http
    port: 80
    targetPort: 3000
  selector:
    name: ref-data-gov-tool-prototype
