---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ref-data-gov-tool-prototype-ingress
  labels:
    stable.k8s.psg.io/kcm.class: default
  annotations:
    ingress.kubernetes.io/secure-backends: "false"
    kubernetes.io/ingress.class: "nginx-external"
    kubernetes.io/tls-acme: "true"
    stable.k8s.psg.io/kcm.provider: http
spec:
  rules:
  - host: {{.HOSTNAME}}
    http:
      paths:
      - backend:
          serviceName: ref-data-gov-tool-prototype-service
          servicePort: http
        path: /
  tls:
  - hosts:
    - {{.HOSTNAME}}
    secretName: ref-data-gov-tool-prototype-tls
